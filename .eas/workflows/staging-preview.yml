# Deploys only a preview alias; production is never promoted by this workflow.
name: Deploy web staging

on:
  push:
    branches:
      - fix/urgent-master-batch-20260925

jobs:
  deploy_web_staging:
    name: Deploy web staging
    type: deploy
    environment: preview
    params:
      alias: staging
