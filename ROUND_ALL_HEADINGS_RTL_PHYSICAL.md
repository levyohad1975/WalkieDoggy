# RTL heading anchoring follow-up

- Replaces `alignSelf: flex-end` on headings/labels with `width: 100%` + explicit right text alignment.
- This avoids logical flex-end mirroring under RTL and anchors text physically to the right edge.
